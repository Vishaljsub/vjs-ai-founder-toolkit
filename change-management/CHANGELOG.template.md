# Changelog — {{APP_NAME}}

What shipped, and what broke + how it was fixed. Newest first.
Forward-looking work lives in [`BACKLOG.md`](BACKLOG.md); deploy history in [`DEPLOYMENTS.md`](DEPLOYMENTS.md); the *why* behind decisions in [`docs/decisions.md`](docs/decisions.md).

Conventions: each `Fixed` entry carries a one-line **root cause** so this doubles as a debugging knowledge base. `Deployed` notes which environment(s) received the change.

---

## YYYY-MM-DD

### Added
- **Feature name.** One or two sentences: what it does, why, and the key implementation detail a future reader would want (file/module names, non-obvious design choice).

### Fixed
- **Bug summary.** Root cause: *the specific, one-line reason it happened.* Fix: what changed.

### Changed
- Behaviour/rename/refactor changes worth flagging.

### Deployed
- Which environment(s) received this change, and the commit/ref.

---

## Copy-paste this section for each new dated entry.
